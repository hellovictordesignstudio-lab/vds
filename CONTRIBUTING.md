# Contributing to VDS

We welcome contributions from the community. Please read this guide before submitting a PR.

## Development Setup
1. Fork the repository
2. Clone your fork
3. Run `pnpm install`
4. Run `pnpm dev` to start the docs site locally

## Commit Convention
We use conventional commits: `feat:`, `fix:`, `docs:`, `chore:`
